events {
    worker_connections 1024;
}

http {
    upstream bdc-competitive {
        server bdc-competitive:3000;
    }

    upstream bdc-assessment {
        server bdc-assessment:3001;
    }

    upstream bdc-dashboard {
        server bdc-dashboard:3002;
    }

    # HTTP to HTTPS redirect
    server {
        listen 80;
        server_name vandoko.com www.vandoko.com competitive.vandoko.com assessment.vandoko.com bdcapp.vandoko.com;
        return 301 https://$server_name$request_uri;
    }

    # HTTPS server for BDC Competitive
    server {
        listen 443 ssl http2;
        server_name competitive.vandoko.com;

        ssl_certificate /etc/nginx/ssl/cert.pem;
        ssl_certificate_key /etc/nginx/ssl/key.pem;

        location / {
            proxy_pass http://bdc-competitive;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }

    # HTTPS server for BDC Assessment
    server {
        listen 443 ssl http2;
        server_name assessment.vandoko.com;

        ssl_certificate /etc/nginx/ssl/cert.pem;
        ssl_certificate_key /etc/nginx/ssl/key.pem;

        location / {
            proxy_pass http://bdc-assessment;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }

    # HTTPS server for BDC Dashboard (Main App)
    server {
        listen 443 ssl http2;
        server_name bdcapp.vandoko.com;

        ssl_certificate /etc/nginx/ssl/cert.pem;
        ssl_certificate_key /etc/nginx/ssl/key.pem;

        location / {
            proxy_pass http://bdc-dashboard;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }

    # Main domain redirect
    server {
        listen 443 ssl http2;
        server_name vandoko.com www.vandoko.com;

        ssl_certificate /etc/nginx/ssl/cert.pem;
        ssl_certificate_key /etc/nginx/ssl/key.pem;

        # Redirect to BDC Dashboard app
        return 301 https://bdcapp.vandoko.com$request_uri;
    }
}